<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NotesSaver</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        /* Custom Dark Mode Styling */
        body.dark {
            background-color: #1a202c;
            color: #cbd5e0;
        }

        .btn-dark-mode {
            background-color: #4a5568;
            border-color: #4a5568;
            color: #cbd5e0;
        }

        .task.dark-mode {
            background-color: #2d3748;
        }

        .task.dark-mode:hover {
            transform: scale(1.05);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.4);
        }
    </style>
</head>

<body class="dark">
    <div class="container mx-auto py-16">
        <h1 class="text-4xl font-bold text-white text-center mb-8 animate-pulse">Notes Saver</h1>
        <div class="flex justify-center">
            <div class="w-full md:w-2/3 lg:w-1/2">
                <div class="bg-gray-800 rounded-lg shadow-lg p-8">
                    <form action="/create" method="post">
                        <input class="w-full mb-4 px-4 py-3 rounded-md bg-gray-700 focus:outline-none focus:bg-gray-600"
                            type="text" placeholder="Title goes here" name="title">
                        <textarea class="w-full mb-4 px-4 py-3 rounded-md bg-gray-700 resize-none focus:outline-none focus:bg-gray-600"
                            placeholder="Write your notes details" name="details"></textarea>
                        <button class="w-full bg-gradient-to-r from-blue-900 to-blue-700 hover:from-blue-700 hover:to-blue-500 text-white font-bold py-2 px-4 rounded-md"
                            type="submit">Create Notes</button>
                    </form>
                </div>
            </div>
        </div>
        <div class="mt-12">
            <h2 class="text-2xl font-bold text-white mb-4 text-center">Your Notes</h2>
            <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">
                <% if(files.length > 0){ %>
                <% files.forEach(function(val){ %>
                <div class="bg-gray-800 rounded-lg shadow-lg p-6 relative task dark-mode">
                    <h3 class="text-lg font-semibold mb-2"><%= val %></h3>
                    <div class="flex justify-between items-center mt-4">
                        <a href="/files/<%= val %>"
                            class="text-blue-500 hover:text-blue-700 font-semibold">Read More</a>
                        <a href="/edit/<%= val %>"
                            class="text-gray-500 hover:text-gray-700 font-semibold">Edit Filename</a>
                    </div>
                </div>
                <% }) %>
                <% } else { %>
                <div class="bg-gray-800 rounded-lg shadow-lg p-6">
                    <h3 class="text-lg font-semibold text-gray-300">No Notes yet.</h3>
                </div>
                <% } %>
            </div>
        </div>
    </div>
</body>

</html>
